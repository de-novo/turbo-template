# Hoist coverage providers so per-package `vitest run --coverage` finds
# them via Node module resolution without each package having to declare
# the dep explicitly.
public-hoist-pattern[]=@vitest/coverage-*
confirmModulesPurge=false
