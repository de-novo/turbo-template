# Copilot Adapter

Use `AGENTS.md` as the repository entrypoint. For detailed rules, read `.agents/manifest.json` and
`.agents/rules/**`.

This adapter is intentionally thin. The canonical rule source is `.agents/rules/**`.
