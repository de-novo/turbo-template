# Claude Adapter

Read `AGENTS.md` first, then `.agents/manifest.json`, then the rule files listed under
`.agents/rules/**` in manifest priority order.

This adapter is intentionally thin. The canonical rule source is `.agents/rules/**`.
